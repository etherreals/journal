const styles = theme => ({
  root: {
    width: '100%',
    marginTop: theme.spacing.unit * 3,
    overflowX: 'auto',
    position: 'relative',
  },
  table: {
    minWidth: 700,
  },
});

export default styles;
