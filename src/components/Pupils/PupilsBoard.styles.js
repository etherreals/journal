const styles = () => ({
  root: {
    flexGrow: 1,
    height: 'calc(100vh - 16px)',
  },
  filter: {
    flexGrow: 1,
  },
  board: {
    flexGrow: 5,
    overflow: 'auto',
  },
});

export default styles;

